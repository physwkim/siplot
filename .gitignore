/target
Cargo.lock

# macOS Finder metadata.
.DS_Store

# Output of the save_graph example.
/egui-silx-graph.png

# Local upstream/reference clones (each has its own .git); not part of this repo.
/egui
/fastplotlib
/silx
/wgpu-native
